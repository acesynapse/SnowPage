name: Site Fonts
description: Base fonts for the SnowPage theme
type: core

form:
  fields:

    body-font:
      type: input.fonts
      label: Body Font
      default: 'publicsans, sans-serif'
    heading-font:
      type: input.fonts
      label: Heading Font
      default: 'publicsans, sans-serif'
