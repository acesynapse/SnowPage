{% extends '@nucleus/partials/particle.html.twig' %}

  {# Query Posts #}
  {% set query_parameters = {
    'post_type': 'mec_calendars',
	'orderby': 'date',
	'order': 'asc',
	'posts_per_page': 1
  } %}


  {% set posts = wordpress.query_posts(query_parameters) %}

  {% set id = post.id %}
  {% set one = '[MEC id="' %}
  {% set two = '"]' %}

  {% block particle %}


<div class="sp-news-title"><span class="sp-news-title-icon" uk-icon="icon: more-vertical; ratio: 2"></span>Upcoming Programs</div>
{{ posts|join(', ') }}
{{ one ~ id ~ two }}
{% endblock %}
